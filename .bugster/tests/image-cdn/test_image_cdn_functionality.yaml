name: Test image CDN functionality
page: /image-cdn
page_path: app/image-cdn/page.jsx
task: Verify images load properly with CDN integration
steps:
- Navigate to the image CDN page
- Verify images are visible
- Check that image dimensions are displayed correctly
- Resize browser window to test responsive behavior
- Verify images adapt to new screen size
expected_result: Images should load correctly with appropriate sizing
